
import { Route, Router } from 'react-router-dom';

function App() {
  return (
    <Router>
      <Route path='/' element={<></>} />
      <Route path='/login' element={<></>} />
      <Route path='/register' element={<></>} />
    </Router>
  );
}

export default App;
